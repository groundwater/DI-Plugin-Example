Play [2.0] with Dependency Injection
====================================

This example pairs dependency injection with Play plugins.

 1. Run `sbt run` to inject the full plugins
 2. Run `sbt test` to inject test plugins
